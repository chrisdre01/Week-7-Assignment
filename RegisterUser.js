import { lazy } from 'react';
import React from 'react';

const MarkdownPreview = lazy(() => import('./RegisterUser.js'));

function RegisterUser() {
    return (
        <>
            <h3>Register User</h3>
        </>
    )
}
export default Page2;
